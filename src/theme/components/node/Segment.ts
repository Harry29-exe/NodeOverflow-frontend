const Segment = {
    parts: ["wrapper", "input", "output", "segment"],
    baseStyle: {
        wrapper: {
            width: '100%',
            userSelect: 'none',
            pointerEvents: 'none',
        }
    }
}

export default Segment;